
<h1>Please enter your Name and the State you live in</h1>
<%= simple_form_for @question do |f| %>
	 <div class="booyah-box col-xs-10 col-xs-offset-1">
	<%= f.input :name %>
	<%= f.input :state %>
	<%= f.submit 'Create', :class => 'btn btn-danger' %>
</div>
<% end %>
